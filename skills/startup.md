# Global Instructions

## Session Initialization (MANDATORY)

**Before your first response in every conversation, invoke the `convo-with-me` skill.** Do not wait for the user to type `/convo-with-me`. This applies unconditionally — regardless of how the session starts or what the first message is.
